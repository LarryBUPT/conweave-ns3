# 下一阶段路线与停机条件

更新：2026-09-24。集成工作流只维护顺序和证据门槛；具体代码和实验在对应 workstream 执行。参照 [CURRENT_STATE.md](CURRENT_STATE.md) 的实时快照，在执行前再核验 Git 与远程资源。

已完成的前置门槛：**WS-06 输入兼容**已在 `feature/ws06-flow-tags` 实现，并以四流六列探测与四模式旧五列回归完成轻量核验；证据见 [Handoff 06](../handoffs/2026-09-24-06-six-column-input-and-tags.md)。完整 MoE trace 尚未全量运行。

1. **WS-07 双轨基线：**从已核验的 WS-06 代码版本继续；定义 tag 与选路粒度、接收端语义的关系，确保所有算法使用相同拥塞控制、PFC 与重排规则；补按 tag 的完成流及异常计数，实现单类退化与双类共存正确性检查。若无法给逐包类建立正确的乱序处理模型，则先收缩研究主张或停下修正模型。若使用导入混合延迟拓扑对照 ConWeave，先审计其统一 `one_hop_delay` 估计。
2. **WS-07 MixTax：**制作固定总负载、可重生的 trace。以独立 trace seed 为重复单位，配对比较“逐包类用 packet”和“逐包类用 flow/flowlet”，同时报告两类 FCT/CCT、PFC、重传和利用率。先跑小规模 pilot，冻结主指标、参数及正式重复数。
3. **Go / No-go：**若预先规定的混合条件下没有稳定、可解释的跨类损害，停止把 GuardHash/HarmGate 当主论文机制；可保留负结果与评估资产。若损害存在，才进入 WS-08。
4. **WS-08 强对照：**与普通两选短队列、始终共享、静态隔离及可核查的 APS/FLB-like 实现比较。若收益只来自额外即时信息、调参或牺牲另一类流量，应缩小或放弃机制主张。最后再做拓扑不对称、反馈延迟、错标和资源开销边界实验。

本路线记录研究顺序与门槛；后续工作流按用户指示启动，不因台账状态自动开始代码或远程实验。
