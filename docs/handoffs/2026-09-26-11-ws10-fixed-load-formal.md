# Handoff 11：WS-10 固定总字节 MixTax 正式现象复核

日期：2026-09-26（中国时间）。来源任务：[WS-10 固定总字节与多 seed MixTax 复核](codex://threads/01a0da7c-8d4d-7522-b1c9-1bfb967bd170)，任务 ID `01a0da7c-8d4d-7522-b1c9-1bfb967bd170`；不能用本交接替代原始数据。

## 1. 本对话目标

在 [预注册 v1.1](../research/ws10-fixed-load-prereg-v1.md) 的单一源码和资源预算下，完成六格 pilot、五 trace seed × 六格正式配对，解释中断与恢复，按原判据报告正负结果并更新项目状态。用户要求运行期间静默定时检查，完成或异常时才更新。

## 2. 已确认的项目事实

个人 fork `feature/ws10-fixed-load-mixtax` 的正式仿真源码 SHA 为 `aa778ac523bc0319999395dd3cf8085b41e73a98`；远端为固定 SHA 的隔离副本，并行编译 `-j2`、同一时刻仅一个仿真。两个参考 remote 只读。五个正式 trace seed `20261001`–`20261005` 与资源 pilot seed `20260926` 分离。每 seed 256 条目标 MoE、背景 0/2/4、两路由模式六格，每格提供 35,651,584 B。原始 ID、SHA 和数值见 [正式机器摘要](../research/ws10-fixed-load-formal-summary.json)；摘要由 [核验脚本](../../scripts/verify_ws10_formal.py) 从忽略的 `results/<ID>/` 重算。

## 3. 已完成工作

- 用户要求维持中断任务并降低监控 token 消耗。工具按约五分钟的只读检查观察运行器，未重启在途格；最终正式 30/30 格均有 `verified` 收据。
- 六格资源 pilot 先通过；正式五 seed 全集随后完成。30 格元数据、trace、拓扑、FCT、CNP、PFC、uplink、目标及 tag 完成率、资源收据经版本化脚本核对。每格 100% 完成，PFC 文件均为空；源码 SHA 一致。详细统计见 [报告](../research/ws10-fixed-load-formal-report.md)。
- SSH 端口转发警告污染一次状态 JSON，产生 `stopped`；该格仿真实际成功，但 RSS 收据不完整。诊断 ID `20260926-070414-ws10-20261002-b4-p` 排除，正式重跑 ID `20260926-071440-ws10-20261002-b4-p` 纳入矩阵；两者原始 FCT SHA 完全一致。此处是控制器解析故障，不是仿真失败。
- 五 seed 主 4 档交互为 1 正 4 负；归一化中位数 −1.3947%，范围 −6.9391% 至 +0.9980%。背景安全 10/10 通过，2 档五个交互均正但与 4 档非单调。预注册主现象 **no-go**。

## 4. 已形成的设计决策

沿用运行前冻结的 4 档主判据，不因 2 档的正向结果事后换主终点。理由是防止结果驱动选档；预注册已把 2 档定义为剂量检查。诊断格只作恢复一致性证据，不计作第六个 seed 或第 31 个正式格。WS-11 的效果比较仍受 [ADR-006](../decisions/ADR-006-conditional-guardhash-selection.md) 和 [ADR-007](../decisions/ADR-007-guardhash-prototype-before-efficacy.md) 的条件约束，本轮未启动。

## 5. 当前状态

WS-10 在预注册的固定总字节、五 seed 正式现象复核范围内完成，结论 no-go。此交接和项目状态的提交会移动分支 HEAD；效果数据仍归属于上面的固定 SHA。论文项目根目录不是 Git 仓库，个人 fork 承载版本化脚本、摘要、报告与状态。实际分支和工作树状态以集成完成后的 Git 检查为准。

## 6. 未解决问题

必须保留：本场景没有达到稳定 4 档正向损害门槛，因此不能声称 GuardHash 减轻了该损害。可研究：2 档正向与 4 档负向的非单调成因，以及其他预先冻结的新场景；新场景需要新的独立契约与结果，不能补入本矩阵。原始 NACK/超时没有独立可靠计数器，标为不可得；每格背景 P99 仅 2 或 4 个样本。

## 7. 后续推荐动作

先把本轮 no-go 纳入论文问题界定与局限，明确 WS-11 当前条件不满足。若仍需机制收益主张，应先提出新的、可证伪的现象假设并预注册独立验证，之后再决定是否开启 WS-11；不能在本轮数据上改判据或择 seed。WS-12 可按解释性负结果路线整理证据。

## 8. 与其他工作流的关系

WS-06 提供六列 tag 输入；WS-07 提供 `dualtrack` 和目标统计；WS-08 确立共同 PFC=0/IRN=1 接收条件；WS-09 的 GuardHash/HarmGate 是已核验工程原型，但不是本轮比较对象。WS-10 只比较 `fecmp` 与 `dualtrack` 的跨类现象，不重写 WS-05 四模式历史 baseline，也不推导 GuardHash 效果。

## 9. CONTEXT SNAPSHOT

`feature/ws10-fixed-load-mixtax` 的正式数据固定在 `aa778ac...`；pilot 6/6、正式 30/30，正式 seed `20261001`–`20261005`。机器摘要列齐所有实验 ID 与原始哈希；主 4 档只有 1/5 正向，中位数 −1.3947%，全类完成与背景安全通过，故 WS-10 现象 no-go。一次 SSH 状态解析异常已通过同 SHA/trace 重跑并匹配 FCT，诊断 ID 排除。WS-11 效果门槛未打开；后续任何新场景需另行预注册。原始数据位于本机个人 fork 的忽略 `results/<ID>/` 与远端 `/home/fnl/lzy/results/<ID>/`。

## 集成交接核验（2026-09-26）

集成任务独立重跑 `python scripts/verify_ws10_formal.py`，返回码 0；重新读取 30 个正式格的元数据、原始文件、哈希和资源收据，判定仍为 `phenomenon_go=false`、4 档 1/5 正向、中位数 `−1.3947001394700151%`。核验前本地及个人 fork 同名远程分支均为 `6cc47c6cffa3d00f7cb3ec71411c68844f57ff76`、工作树干净。WS-10 在预注册范围内无未结实验格，可以归档；完整 16,384 条 MoE 输入是 WS-11 独立新场景，不回填 WS-10 判据。
